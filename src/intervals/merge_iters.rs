use super::Interval;

pub fn merge_2_interval_iters<T>(_a: &Vec<Interval<T>>, _b: &Vec<Interval<T>>) -> Vec<Interval<T>>
where
    T: Copy + Ord,
{
    todo!()
}

pub fn merge_interval_iters<T>(_input: &Vec<Vec<Interval<T>>>) -> Vec<Interval<T>>
where
    T: Copy + Ord,
{
    todo!()
}
