use super::Interval;

pub fn sort_by_start<T>(_v: &mut Vec<Interval<T>>)
where
    T: Copy + Ord,
{
    todo!()
}
pub fn sort_by_end<T>(_v: &mut Vec<Interval<T>>)
where
    T: Copy + Ord,
{
    todo!()
}
